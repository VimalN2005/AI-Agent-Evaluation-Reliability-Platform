version: '3.8'

services:
  eval-platform:
    build: .
    container_name: ai-agent-eval-platform
    ports:
      - "8501:8501"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
    restart: unless-stopped
